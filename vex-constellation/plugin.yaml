manifest_version: 1
name: vex-constellation
version: 1.1.0
description: VEX Constellation — inter-agent protocol on port 8390 with persistent inbox and autonomous responder bridge.
author: NEXUS VEX + Hermes VEX · Axis Dynamics
license: MIT
provides_hooks:
  - on_session_start
  - on_session_end
requires:
  hermes: ">=0.11.0"
